package main

import "fmt"

func main(){
    
    var name string = "Roman"
    var city, state string = "Phoenix", "Arizona"
    fmt.Println("name : ", name)
    fmt.Println("city : ", city, " state : ", state)
}
